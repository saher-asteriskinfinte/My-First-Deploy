<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-6">
        <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block ml-2 mt-2">Log-out</a>
        <div class="flex">
        <a href="/profile/upload">
        <div class="w-12 h-12 rounded-full mt-[0.2rem] items-start gap-3">
        <img class="w-full h-full object-cover rounded-full" src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>
         </a>
        <h2 class="text-white text-5xl mb-2 ml-2">Hello, <%= user.name %>  👋🏻</h2>
        </div>
         <h5 class="ml-2 mb-4">You can create a new post.</h5>  

         <form action="/post" method="post" class="pl-2">
            <textarea name="content" placeholder="What's on your mind ?" class="resize-none bg-transparent border-2 border-zinc-700 p-4 rounded-md w-1/3 h-16 outline-none mb-4 block"></textarea>
        <input type="submit" value="Create a new post"class="px-8 py-2 bg-blue-500 rounded-md block"> 
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400 mb-2 ml-2">Your Posts</h3>
            <div class="postcontainer">
            <% user.posts.reverse().forEach((post)=>{ %>
            <div class="post w-1/3 p-4 border-2 border-zinc-800 mb-4 bg-zinc-800">
                    <h4 class="text-blue-500 mb-2">@<%= user.name %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block "><%= post.likes.length %> Likes</small>
                    <div class="btns flex gap-4 ">
                        <a href="/like/<%= post._id %>" class="text-blue-500">
                        <%= post.likes.indexOf(user._id) === -1 ? "Like" :"Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="text-zinc-400">Edit</a>
                    </div>
                </div>
            <%  }) %> 
        </div>
        </div>
        </div>
    </div>
</body>
</html>