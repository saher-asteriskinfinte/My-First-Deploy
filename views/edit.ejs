<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-6">
         <h5 class="ml-4 mb-4 ">Edit your post.</h5>  
         <form action="/update/<%= post._id %>" method="post" class="pl-2">
            <textarea name="content" placeholder="What's on your mind ?" class="resize-none bg-transparent border-2 border-zinc-700 p-2 rounded-md w-1/3 h-16 outline-none mb-4 block">
            <%= post.content %>
            </textarea>
        <input type="submit" value="Update post"class="px-8 py-2 bg-blue-500 rounded-md block"> 
        </form>
</div>
</body>
</html>
